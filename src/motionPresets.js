/** Общий easing для «мягких» появлений (как в History / Timing). */
export const EASE = [0.22, 1, 0.36, 1];

/** Стандартный viewport: один раз, при входе в зону видимости. */
export const VIEWPORT = {
  once: true,
  amount: 0.22,
  margin: '0px 0px -8% 0px',
};
